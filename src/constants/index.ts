import { paths } from './paths'
import { routes } from './routes'
import { BASE_URL } from './urls'
import type { pathType, routeType } from './types'

export { routes, paths, routeType, pathType, BASE_URL }
